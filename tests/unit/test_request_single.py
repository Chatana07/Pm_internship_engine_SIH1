# Let's modify the ML model to allow requesting a specific number of recommendations
# This would require changes to the API endpoint to accept a top_k parameter
print("To test single recommendation, we would need to modify the API to accept a top_k parameter.")
print("The current implementation always requests 3 recommendations.")